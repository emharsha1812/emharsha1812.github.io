services:
  jekyll:
    image: amirpourmand/al-folio:slim
    ports:
      - 8080:8080
      - 35729:35729
    volumes:
      - .:/srv/jekyll
      - bundle_cache:/usr/local/bundle   # <-- cache gems
    environment:
      - JEKYLL_ENV=development

volumes:
  bundle_cache:
